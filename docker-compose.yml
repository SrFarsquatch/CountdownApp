# CasaOS custom-install file.
# GitHub Actions builds and publishes the image from main.

name: countdownapp

services:
  countdown:
    image: ghcr.io/srfarsquatch/countdownapp:edge
    pull_policy: always
    container_name: countdownapp
    ports:
      - "8088:8080"
    environment:
      TZ: "America/Vancouver"
      GOOGLE_CLIENT_ID: "${GOOGLE_CLIENT_ID:-}"
      GOOGLE_CLIENT_SECRET: "${GOOGLE_CLIENT_SECRET:-}"
      APP_BASE_URL: "${APP_BASE_URL:-}"
      APP_SECRET: "${APP_SECRET:-}"
      UPDATER_URL: "http://countdown-updater:8090"
      UPDATE_TOKEN: "${UPDATE_TOKEN:-countdown-internal-updater}"
    volumes:
      - type: bind
        source: /DATA/AppData/$AppID/data
        target: /data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    mem_limit: 192m
    mem_reservation: 32m
    cpus: "0.35"
    pids_limit: 64
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
    x-casaos:
      volumes:
        - container: /data
          description:
            en_US: Persistent CountdownApp data
      ports:
        - container: "8080"
          description:
            en_US: Countdown web interface and FrameOS API

x-casaos:
  id: countdownapp
  store_app_id: countdownapp
  architectures:
    - amd64
    - arm64
  main: countdown
  category: Utilities
  developer: SrFarsquatch
  author: SrFarsquatch
  tagline:
    en_US: Self-hosted countdown, calendar and e-ink dashboard
  description:
    en_US: Manage countdowns, read Google Calendar events, and publish a private FrameOS-friendly e-ink feed.
  scheme: http
  port_map: "8088"
  index: /
  title:
    custom: Countdown
    en_US: Countdown
  version: "edge"
